spec:
  name: bullet-train
  services:
    - name: web
      github:
        repo: gazayas/bullet_train
        branch: digital-ocean-deployment
  databases:
    - name: bullet-train-production
      engine: PG
      production: true
      cluster_name: bullet-train-db-cluster
      db_user: untitled_application
      db_name: untitled_application_production
