spec:
  name: bullet-train
  services:
    - name: web
      github:
        repo: gazayas/bullet_train
        branch: digital-ocean-deployment
  databases:
    - name: bullet-train-production
      engine: PG
  databases:
    - name: bullet-train-redis
      engine: REDIS
