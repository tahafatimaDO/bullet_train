spec:
  name: bullet-train
  services:
    - name: web
      github:
        repo: gazayas/bullet_train
        branch: digital-ocean-deployment
  databases:
    - name: db
      engine: PG
      production: true
